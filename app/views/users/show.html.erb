<% provide :title, !@user.nil? ? @user.name : t(".not_exits") %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <% if @user %>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
      <% end %>
    </section>
  </aside>
</div>
